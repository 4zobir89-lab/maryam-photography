# Development environment — safe to commit.
# Production secrets are set on Vercel dashboard, NOT here.
# NEXT_PUBLIC_SITE_URL intentionally not set here so the fallback
# (maryam-photography.vercel.app) is used in production builds.
DATABASE_URL=file:/home/z/my-project/db/custom.db
AUTH_SECRET=dev-only-secret-key-not-for-production
